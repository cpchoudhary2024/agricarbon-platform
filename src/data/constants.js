/** Physical + unit constants. These are definitions, not estimates. */

/** Molecular mass ratio CO2 : C = 44/12. Converts mass of carbon to mass of CO2e. */
export const C_TO_CO2 = 44 / 12; // 3.6667

/** 1 hectare = 2.4710538 acres. */
export const ACRES_PER_HECTARE = 2.4710538;

/** A "ton" in a US agricultural carbon contract means a METRIC ton (1,000 kg) of CO2e. */
export const TON_LABEL = 'metric ton CO₂e';
